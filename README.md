# ViT-FineTuning-CIFAR10
Fine-tuning the Vision Transformer (ViT) model on the CIFAR-10 dataset using the Hugging Face Transformers library. The project demonstrates dataset preprocessing, model training, evaluation, and inference, comparing pretrained and fine-tuned performance for image classification.
